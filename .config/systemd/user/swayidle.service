[Unit]
PartOf=graphical-session.target
After=graphical-session.target
Requisite=graphical-session.target

[Service]
ExecStart=/usr/bin/swayidle -w timeout 900 'swaylock -f' timeout 1200 'niri msg action power-off-monitors' timeout 1800 'systemctl suspend' before-sleep 'loginctl lock-session'
Restart=on-failure
